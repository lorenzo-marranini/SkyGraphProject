Guest:

1) Cercare info su voli in base a data (default = today) , origine*, destinazione* xy 
2) Visualizzare le posizioni in tempo reale dei voli attualmente in volo (mappa real-time)  xy 


Traffic Controller:

Airlines statistics
1) Mongo = AVG Delay of Airlines xy
2) Mongo = Total number of flights per Airlines xy
3) Mongo = Total KM flown by airlines xy
4) Mongo = Given a time range* view the AVG flight distance of airlines xy

On a specific Route
5) Mongo = Given origin* and destination* view number of flights of airlines x
6) Mongo = Given origin* destination* view avg delay of airlines x

tutte le precedenti hanno come parametro anche il range di tempo 
(last day, last week, last montH)

Other Functionalities
7) Mongo = Given origin* destination* view airports to land in case of Emergency xy
8) Neo4j = Given origin* destination* view alternative routes if an airport is unavailable xy


Airline Representative:

Strategy Business on routes:
1) Neo4j + Mongo = Given origin* destination* stops*(scali) and date* (default=today), find the possible flights with less time flown. xy = QUICKEST REAL 
2) Mongo = Given a time range * View the most frequent route  xy 
2.1) Mongo = Given a time range * View the most diverted route xy
2.2) Mongo = Given a time range * View the most cancelled route xy 

on Airports
4) Neo4j = View betwenness centrality of airports xy
5) Mongo = View airports order by avg delay xy
6) Neo4j = View airports order by most connections xy

on Cities:
7) Neo4j = view ranking of cities per most traffik xy // NUM_VOLI
8) Neo4j = view statistic of a single city xy 

Others:
9) Mongo = Given a time range* view statistic of delay per day of week   xy
10) Mongo = Given an Airline* and time range* view total km flown, avg delay, total # of flights, AVG KM flown, effincency xy



Administrator:
1) view all users and update them x